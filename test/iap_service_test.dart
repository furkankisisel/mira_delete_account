import 'package:flutter_test/flutter_test.dart';

void main() {
  test('iap mock test placeholder', () async {
    // Bu testte gerçek store çağrıları yapılmaz; IAPService HTTP çağrıları
    // mock'lanmalı ve purchase akışı simüle edilmelidir.
    expect(1, 1);
  });
}
