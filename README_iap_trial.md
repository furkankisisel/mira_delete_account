# Subscription/IAP Documentation Removed

This documentation was removed as part of cleaning up subscription and premium features from the project. If you reintroduce billing in the future, recreate this documentation based on your new IAP implementation.

Original file: `README_iap_trial.md` (archived).
Backend stub:
```bash
cd backend_stub
npm install
npm start
```

> Not: Backend URL `lib/config/constants.dart` içinde `http://localhost:3000` olarak ayarlı. Cihaz/emülatörden erişim için platforma göre `10.0.2.2` (Android Emulator) ya da yerel IP gerekebilir.
